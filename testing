leaks:
	- map without images
	- invlaid map with multiple starting positions

map validation:
	- segfault on passing incorrect map name
	- if map start with 0, - valid ?
	- if map start with 0 and then new lines - valid ?

passing same texture multipal time

- sys use memset
- color and texture parsing -- done
- parsing color without value or with less that 3 value leads to segfault -- done
- 0 before map -- done
- some random text before map -- done
- double color on texture --done
- color more than the argument --done
- if img not found -- done
- texture load fail leaks -- done
- conditional jump for invalid map -- done
- color minus value leak -- done

